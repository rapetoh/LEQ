# Public surface of LEQ. Copy to .env.local and fill in.
# The publishable key is public by design: every rule is enforced by RLS in the database.
VITE_SUPABASE_URL=https://gnabuebxleogsuhvdgpk.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=
# Store link shown at the end of a duel. Leave empty until the app is published.
VITE_LIEN_APPLICATION=
