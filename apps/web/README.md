# @leq/web (Phase 7)

Public web surface of LEQ, not started yet. It will hold:

- the duel invitation flow: the invitee listens to the inviter's take, records a reply in the browser (MediaRecorder), uploads it as an anonymous principal and sees the verdict, without installing the app;
- the privacy and terms pages;
- the install link.

It is created in Phase 7 (Arena and duels). See docs/STATUS.md for the build order.
